# Report: mars12 onboarding (Track 43) — 2026-07-28

Box named only as `mars12`. Evidence logs: `analysis/mars-install/logs/` (redacted:
no address, no secret). **Result: harmonised and registered; the install is red at
an upstream dependency, not at mars12.**

**(a) Harmonisation — verified.** One password login (attempt 1 of 2; password only
via `SSHPASS="$(awk … register)"`, never in a command line, file or log), then key
only. `nuc` created (uid 1001, venus10's groups), shared key in
`~nuc/.ssh/authorized_keys` (700/600, fingerprint == local `id_ed25519.pub`),
`/etc/sudoers.d/90-nuc-nopasswd` (440 root:root, `visudo -cf … parsed OK`),
`sudo -n id -un` → `root`. Hostname was **already** `mars12` on arrival, so the
"hostname before k3s" rule is satisfied, not merely planned. Delivery user deleted
only afterwards (`userdel -r`); `nuc` is the only human user. Console password set
to the fleet value via `chpasswd` from the process environment (`passwd -S nuc` →
`P`) — my own deletion had removed the only console-capable account. Root LV
100 G → 473.89 G (`lvextend -l +100%FREE`): 436 G free — the venus10 installer trap.

**(b) Register.** `MARS12_` `KIND`·`SSH_HOST`·`SSH_USER`·`SSH_KEY`·`SSH_ALIAS`·
`ENV_PROFILE`·`CHECKOUT`·`NOTES` + comment block with the box baseline. `LIFECYCLE`
removed: the draft value `onboarding` is not in the schema (`registry.py:59-61`),
and since `load()` validates every instance it made the register unreadable
**fleet-wide** (`selector`, `probe` both exit 2). Now green, exit 0.

**(c) Install.** Route: the pipeline via the selector, never a rebuild.
`selector deliver mars12 --sha 1a2c51dc7891` (exit 0) gave a real `.git` from the
start (8821 tracked == 8821 files); `vpath_agents` at the *declared* pin `66b0634`
/v4.5.1, not the Mac's v4.5.3; `.env`+`cosign.key` streamed, 600, byte-identical
(sha256); overlay touched only `NUC_HOST`/`VPATH_HOST`. Phases: `bootstrapVMs`
`BUILD SUCCESSFUL in 1m 16s`; `syncWorkspace` green — `[ok] Full-tree identity
verified: /workspace is a complete git checkout at 1a2c51dc7891 (8821 tracked
files, 474 extra/*.py match host)`; **`provisionBuildVM` FAILED** after 18.5 s at
`==> Node.js`; the two later phases did not run (chain stops at the first red).
Last real lines: `curl: (22) The requested URL returned error: 403` /
`Task '${name}' command finished with non-zero exit value 22`.

Root cause, measured: `vm_infra/scripts/provision-build-host-nuc.sh:191` curls
`https://deb.nodesource.com/setup_22.x` → **403 from the Mac *and* from mars12**
(upstream, not box-specific). The `node_20.x`/`node_22.x` `Release` files and the
`gpgkey` used by `platform_infra/docker/Dockerfile.node:35` are 403 too. **HEAD does
not fix it** (0 commits since the pin touch that file), so installing HEAD would
have failed identically. Sister NUCs are unaffected only because the step
short-circuits on an already-present node (v22.22.2 / v22.23.1). So the fleet looks
green while no virgin box can be built. No workaround applied, per mandate.

**(d) Probe.** mars12 → **UNHEALTHY, exit 2**: checkout `1a2c51dc7891 (main)` and
profile `nuc` establishable, `platform silent`, deployments/marker/image not
establishable. mercury8 + venus10 → HEALTHY, exit 0, both `1a2c51dc7891`, **53/53**
deployments: the commit I installed is the fleet-uniform one.

**(e) Open.** 1) Blocker: nodesource 403 blocks every from-zero install fleet-wide —
needs its own lane (host provisioning *and* `Dockerfile.node`); not mine to decide.
2) The server README requires `playbook/nuc-install-mode.md` before a first run —
the file does not exist. 3) `probe.py:273` sets `HEALTHY` as an *initial* value,
downgraded only on positive evidence: mars12 read HEALTHY at exit 2 while nothing
was installed, and UNHEALTHY once more existed. 4) `provisionBuildVM` prints the
unexpanded `Task '${name}'`, so the error cannot name the failing command, and
`[warn] Workspace not found at /workspace — using PWD` continues anyway — a soft
pass. 5) Provisioning needs the internet while the README calls the box airgapped
by design. 6) Not verified: anything past `provisionBuildVM` — no k3s, no docker, no
platform. 7) `uv.lock` is untracked here, not mine, left alone.

**(f) Tools.** Only this repo's own `selector`/`probe`. What paid off:
`selector --print` before every run (it showed my *shell*, not the tool, was
collapsing multi-task args); the EXIT-marker pattern, since `tee` masks exit codes;
per-phase invocations to bound the VPN link's blast radius; and reading the venus10
precedent first — its nine prerequisites and its "`git archive` is not enough"
lesson both held. `sshpass` over a pty did **not** drive `sudo`; feeding `sudo -S`
from the process environment did.
