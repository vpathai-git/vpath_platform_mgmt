# Rohfassung — mars12: Aufnahme, Zugang harmonisieren, Install vorbereiten

Diktat Andre, 2026-07-27 ~22:22. **Redaktion:** Die Zeile mit Adresse,
Nutzername und Passwort-Verweis ist hier bewusst ersetzt — Zugangsdaten stehen
ausschließlich im gitignorierten Register (`instances.local.env`, Block
`mars12`). Kein stiller Drop: die Auslassung ist markiert.

> wir haben einen weiteren NUC aufgesetzt er ist online verfügbar und muss
> auch in die 06 platform mgmt instanzliste aufgenommen werden. Mars12 (weil
> ein Nuc12)
>
> [REDAKTIERT: IP, Nutzername, Passwort-Verweis → Register `mars12`]
>
> Harmonisiere den install (installier den gleichen sshkey für remote zugriff
> und bereite den install vor. => muss alles mit einem Agenten aus dem
> 06-platformanagement erfolgen
>
> jetzt erst mal /user-request anlege in 06 platform management und refernz im
> claas_Demo workspace unter analysis/mars-install/

## Diktat-Dekodierung (nur Wortreparaturen)

| Wortlaut | Gemeint |
|---|---|
| „Mars12 (weil ein Nuc12)" | Signaletik-Name `mars12`: Planet `mars` (der reservierte nächste NUC-Name) + Suffix 12 = NUC-Generation (NUC12-Board), analog `mercury8`/`venus10` |
| „Harmonisiere den install" | die Box auf die Uniform-NUC-Zugangsregel des Registers heben (das venus10-Playbook vom 2026-07-24) |
| „den gleichen sshkey" | den gemeinsamen NUC-Key aus dem Register (`*_SSH_KEY` der bestehenden NUCs) |
| „bereite den install vor" | Install-Vorbereitung bis VOR den eigentlichen Server-Install — kein Install-Lauf |
| „06-platformanagement" | dieses Repository, `vpath_platform_mgmt` (Editor-Folder „06") |
