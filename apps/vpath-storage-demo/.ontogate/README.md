# OntoGate book — vpath-storage-demo

This book composes the 27-contract app hull with the storage demo's concept
plane. All eight app-owned TypeScript entities are mapped to validated, dated,
SHA-pinned concept documents. K01 binds the honest storage explorer render /
empty-state path to its uitest scenario. Run:

    python3 examples/vpath-storage-demo/.ontogate/regenerate.py
    python3 examples/vpath-storage-demo/.ontogate/check.py

Follow the method cycle in the fortune-teller doctrine carrier when extending
the catalog, stories, or proof surface.
