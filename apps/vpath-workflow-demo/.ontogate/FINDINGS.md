# FINDINGS.md — the pin ledger (gap ↔ pin, both directions)

The workflow example has no open hull or concept-plane gaps. New gaps must be
recorded here as explicit `PIN` entries in lockstep with the machine model.

## Open pins

None.
