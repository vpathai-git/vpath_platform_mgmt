# OntoGate book — vpath-workflow-demo

This doctrine carrier composes the 27-contract app hull with the workflow
demo's concept plane. All eight app-owned TypeScript entities are mapped to
validated, dated, SHA-pinned concept documents. K01 binds the honest
standalone configuration and cluster-only empty state to its uitest scenario.
Run:

    python3 examples/vpath-workflow-demo/.ontogate/regenerate.py
    python3 examples/vpath-workflow-demo/.ontogate/check.py

Follow the method cycle in the fortune-teller doctrine carrier when extending
the catalog, stories, or proof surface.
