---
entity: src.components.NewFolderDialog
kind: frontend
stories: []
tests: []
status: reverse_draft
---

# src.components.NewFolderDialog — HYPOTHESIS STUB (reverse_draft)

> Inventoried from `src/components/NewFolderDialog.tsx` at book adoption (2026-07-24). This is a
> placeholder for a concept that has not been excavated — deliberately
> EMPTY, never generated pseudo-content. Work the gap off: write the
> real concept, flip `status: validated` (dated), add the sha pin in
> `spine.yaml` and the dated record in the book ONTOLOGY.md (R-D).
