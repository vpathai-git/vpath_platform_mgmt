# Standalone supervisor builds the app venv from this file, then editable-installs
# the vendored vpath-backend-sdk (which brings fastapi/pydantic/authlib). Curated
# direct deps only (CLAUDE.md §8 — never pip freeze).
httpx>=0.28.1
uvicorn>=0.51.0
