{
  "template_version": "1",
  "kind": "server-nuc",
  "title": "server on a NUC",
  "proven": true,
  "summary": "An on-prem NUC box carrying the full k3s platform. Planet name in acquisition order (mercury8, venus10, ...); the numeric suffix is the NUC generation.",
  "fields": [
    {
      "key": "SSH_HOST",
      "required": true,
      "example": "<lan-address-of-the-box>",
      "comment": "address the dev machine connects to"
    },
    {
      "key": "SSH_USER",
      "required": true,
      "example": "<box-user>",
      "comment": "login user on the box"
    },
    {
      "key": "ENV_PROFILE",
      "required": true,
      "default": "nuc",
      "comment": "server env profile name -> ./gradlew -Penv=<this>"
    },
    {
      "key": "CHECKOUT",
      "required": true,
      "example": "<path-of-the-server-checkout-on-the-box>",
      "comment": "the server checkout ON the box: delivery target and Gradle cwd"
    },
    {
      "key": "SSH_KEY",
      "example": "$HOME/.ssh/<key-file>",
      "comment": "private key path; omit to use the ssh default"
    },
    {
      "key": "SSH_KEY_SOURCE",
      "example": "<credentials-bundle>::<member-name>",
      "comment": "where to get the key if it is not materialised yet"
    },
    {
      "key": "SSH_ALIAS",
      "example": "<~/.ssh/config alias>",
      "comment": "~/.ssh/config alias, informational"
    },
    {
      "key": "LOCATION",
      "example": "<site or room> / <filesystem root on the box>",
      "comment": "location question 1 -- where this instance lives (host + filesystem)"
    },
    {
      "key": "BUILD_PROCESS",
      "example": "on the box: <CHECKOUT>/gradlew -Penv=<ENV_PROFILE>",
      "comment": "location question 2 -- where its build process lives"
    },
    {
      "key": "SOURCE_REPOS",
      "example": "<repo-url> <repo-url>",
      "comment": "location question 3 -- which source repositories the build pulls apps from"
    },
    {
      "key": "IMAGE_REGISTRY",
      "example": "<registry-url or 'local docker daemon on the box'>",
      "comment": "location question 4 -- where the container images live"
    },
    {
      "key": "ONTOGATE_VIEW",
      "example": "http://127.0.0.1:<port>",
      "comment": "base URL of a running OntoGate viewer for this project's Spine; leave unset if none runs -- the console shows a missing view as missing"
    },
    {
      "key": "LIFECYCLE",
      "default": "live",
      "comment": "live | planned -- 'planned' means agreed but not built yet"
    },
    {
      "key": "NOTES",
      "example": "<one line of free text>",
      "comment": "free text, one line"
    }
  ]
}
